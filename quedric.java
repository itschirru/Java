public class quedric {
    public static void main(String[] args) {
        int a=1;
        int b=5;
        int c=3;
        int d= (b*b)-4*a*c;

        if (D>0) {
            double r1= (-b + Math.pow(D,0.5)))/(2*a);
            double r2= (-b - MAth.pow(D,))
        }
    }
}
